# Exclude unpacked binaries from npm package (postinstall unpacks from archives)
tools/unpacked/

# The published package contains only native launchers and explicitly listed
# release artifacts from package.json.
